# Care247 Customer Search Application

This is a web assignment for Care247 — a simple customer search tool built with **React + TypeScript + TailwindCSS**.

## Tech Stack
- React + Vite
- TypeScript
- TailwindCSS
- JSON Server (Mock API)

##  How to Run
1. Install dependencies:
   ```bash
   npm install
